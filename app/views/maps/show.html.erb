<% if @map.gm_is(current_user) %>
	<%= render :partial => 'management' %>
<% end %>

<script>
$(function() {
	var map = new Map("<%= @map.image_url %>", 
		{
			gridX: <%= @map.width %>, 
			gridY:<%= @map.height %>, 
			xOffset:<%= @map.x_offset %>, 
			yOffset:<%= @map.y_offset %>
		});
	var entities = {};
	<%= render :partial => 'entities/show_on_map.js', :collection => @map.entities, :as => :entity %>
	MapObject.opacifyOverlapping();
});
</script>

<%= render :partial => 'entities/show_on_map.html', :collection => @map.entities, :as => :entity %>